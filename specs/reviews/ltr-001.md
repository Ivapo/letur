# Review record — ltr-001 (`specs/web_app_spec.md`)

Append-only. One heading per round, newest first.

### Phase 2 shipped — 2026-09-26 — the gate record

Built on rustc 1.97.1, wasm-pack 0.14.0, macOS, against `9bfbee6` as the commit before.

1. **`cargo test --workspace`: pass** — 110 (`app`), 12 (`app/tests/page_examples_test.rs`,
   one ignored: the bless), 41 (`letur-project`, the two new `download` tests among them).
   `page_examples_test.rs`'s diff is doc comments only.
2. **The pin.** `cargo install --locked md2pdf-cli --version 0.4.0`; `md2pdf --version`
   reports 0.4.0, the `md2pdf-core` `web/Cargo.lock` resolves. **The lockfile**, before the
   seed: 390 names shared with the CLI's `Cargo.lock`, 38 differing (the review measured
   40), `icu_*` among them. Seeded from `md2pdf-cli-0.4.0/Cargo.lock`, then `cargo check`
   added `letur-project` and `console_error_panic_hook` and pruned the CLI's own: **391
   shared, 0 differing.** `web/check.mjs`'s pre-flight holds it.
3. **`web/check.mjs`: every clause (a)–(n) passes in Chromium and in WebKit**, one engine
   per process, the site assembled fresh. Clause (c) held byte equality with the CLI on
   all nine `ok` examples in both engines. Four clauses failed on the first run and each
   was the host's or the check's, not the gate's: (i) the host named the download after
   the whole token (`download:document.md`, sanitised to `download_document.md`) — fixed
   to strip it; (f) and (g) waited with `waitForFunction` over an async predicate, whose
   promise is truthy, so they passed before the first compile — replaced by polling the
   status from Node; (g) then needed a hover, the panel drawing a row's controls only
   under the pointer. (j) passed once (i) was fixed.
4. **The copy**: `diff app/dist/index.html _site/app/index.html` is the one inserted line
   before `</head>`; `git diff --exit-code 9bfbee6 -- app/dist/` is empty. `bun
   app/typecheck.mjs` passes.
5. **The property**: `cargo clippy -p letur-project -- -D clippy::disallowed_methods -D
   clippy::disallowed_types` and `cargo build -p letur-project --target
   wasm32-unknown-unknown` both pass with `Preview::download` in the crate.

**One finding in the spec, taken to the author mid-build and decided there**: §2's table
said `export` and `view-lines` both have buttons. Only `view-lines` does; export is a
desktop menu item with no accelerator, so in a tab it was unreachable and gate (j) had
nothing to press. The author chose the host binding ⇧⌘E; §2 carries the `CORRECTED` note.

**Size**: `web/pkg/letur_web_bg.wasm` 33,239,606 raw and 9,922,721 under `brotli -q 11`;
`9bfbee6`'s `md2pdf_web_spike_bg.wasm` rebuilt the same day, 33,165,169 and 9,900,913 —
exactly the last record. Recorded in `rules/web-app.md`.

**The close-out's own two citations.** This spec's Phase 2 close-out sentence cited
`render` and `anchors` by their `web/src/lib.rs:` path, which `spec-lint` then failed on;
the same path-half-only rewrite it prescribes for `mpdf-006` was applied to it, as
Phase 1's close-out did for this spec's §2. `spec-lint`: 0 errors.

### Round 2 — Phase 2 only — 2026-09-26 — the same three reviewers, resumed — **READY (converged)**

Zero blocking, all three lenses. Every round-1 blocker was verified resolved against the
file, not the changelog: `Preview::download` keeps a download off `Preview::save_as`
and off `MemFiles::save_as`, and moves nothing; the session's `Timer` reads
`performance.now()` and `compile()` takes no argument; the seven `web_demo_spec.md`
citations lose their path half; `web/Cargo.lock` is seeded from the CLI's. The
correctness reviewer confirmed the CLI 0.4.0 lock already carries every version the new
dependencies need (`http` 1.5.0, `serde` 1.0.229, `serde_json` 1.0.151, `wasm-bindgen`
0.2.126), so seeding forces no shared crate off the CLI's version.

Non-blocking, folded in: a dated `CORRECTED` note on §2's "duration passed in", which
Phase 1 shipped as a host `Timer`; the two literals the host words on both hosts
(`save_as_path`'s "no document is open", `current_pdf`'s "the page is out of date")
recorded in §2; `rules/desktop.md` named in the close-out for `Preview::download`; 3(i)
names the `refs.yml` row; 3(n) says how uncaught errors are counted (in-page listeners,
since `page.on('pageerror')` misses what `checks.mjs` records); gate 2 reads the first
glob match with `$CARGO_HOME` defaulting to `~/.cargo`; a new gate 5 re-runs Phase 1's
clippy lint and wasm build, since this phase adds to the crate.

**One number re-measured and disputed**: the exit-gate reviewer counted 391 package
names shared by the two lockfiles, the author 390 (distinct `name =` entries in each,
intersected); both count 40 differing. The gate keys to the property — every shared
package at a version the CLI's lock holds — not to either count, so the record keeps
390 and notes the disagreement here.

On convergence: Phase 2 `reviewed: 2026-09-26`. OQ-2 stays open at its default (no
download link), which the phase builds with.

### Round 1 — Phase 2 only — 2026-09-26 — a fresh panel of three (correctness, exit gate, scope) — **NOT READY (4 blocking, deduplicated)**

**Round 0**, answered by the author: yes — Phase 2 produces the observable directly,
Letur's own window drawing the page beside the text as it is typed, in a tab; and it is
the right first slice of the browser app, since Phases 3 and 4 change only where the
project's files live.

Blockers, and how each was resolved:

1. **A download through Save-as would reach the crate's `save_as`** (all three).
   `MemFiles::save_as` takes `download:x.md` as a root-relative name, writes it into the
   project and moves the pane; and the table's "marks it saved" contradicted
   `Preview::save_as`'s own rule for a save outside the project, which would let the next
   row click load the map's older copy over unsaved text. **Resolved**: the session
   recognises the token first, and Phase 2 adds `Preview::download(name)` — kind refused,
   the buffer's bytes, the crate's receipt, nothing moved — gated by 3(i) and 3(j).
2. **`compile(elapsed_ms)` did not match the seam Phase 1 shipped** (correctness, scope):
   a duration cannot be an argument to the call that runs the compile, and the compiles
   inside `open_document` and `set_edited` would read "0 ms". **Resolved**: a `Timer`
   over `performance.now()` handed to `Preview::new`, and `compile()` with no argument.
3. **Deleting `render` and `anchors` would fail `spec-lint`** (correctness):
   `specs/web_demo_spec.md` cites them seven times, and Phase 1's path-only rewrite does
   not apply to a symbol that is deleted rather than moved. **Resolved**: the close-out
   drops the path half of those seven citations and nothing else, checked by word-diff.
4. **The reference CLI is built from a different dependency graph** (exit gate). Measured
   by the author: md2pdf-cli 0.4.0's published `Cargo.lock` and `web/Cargo.lock` shared
   390 package names and differed on 40, among them every `icu_*` crate (2.2 against
   2.3), and Typst breaks lines with `icu_segmenter`'s data — so a byte mismatch in 3(c)
   could not be pinned on the implementation. `cargo update --precise` per crate failed
   (the `icu_*` requirements interlock); copying the CLI's lockfile into a scratch copy of
   `web/` and letting `cargo metadata` re-resolve left **none** differing. **Resolved**:
   Phase 2 seeds `web/Cargo.lock` that way, and gate 2 refuses to compare unless the
   graphs agree.

Non-blocking, accepted and folded in: `rendered` after every non-read call; the title as
the file name of `edited`; injection before `</head>`, where `serve.mjs` puts it;
refusals thrown as plain strings; `set_main` as `ask_main` then a reopen; `stage` and
`set_appearance` added to the session; `pending_open` waiting for the seed's fetch and an
unknown name seeding nothing; `web/package.json` for Playwright; the landing-page
removals enumerated; the README's web section created; `rules/web-app.md`'s sources
widened; stale comments named; `_site/` ignored; Phase 3's appearance key made global to
match; the "user's own files" note narrowed to markdown; `dialog.save` as a
`window.prompt`; ungated rows gated (hashchange, file input, no-hash seed and title,
appearance, uncaught errors); gate 4 checking `app/dist/` is unchanged; 3(e), 3(f), 3(h)
made exact. **Gate 5, the module's size, became a close-out record**: it could not fail,
and its literals are the last record, not a same-day baseline. **Deferred**: the deploy
itself — the published URLs are opened once and recorded, the gate being the local site.
**Rejected**: none.

### Phase 1 shipped — 2026-09-25 — the gate record

**Gate 1, the count.** `cargo test --workspace` on `7c24336`, the commit before the
phase: 156 tests — `letur` 142 passed and 1 ignored, `page_examples_test` 12 passed and
1 ignored. After: 163 — `letur` 110 passed, `page_examples_test` 12 passed and 1
ignored, `letur-project` 39 passed and 1 ignored. **No fewer, and every name that left
`app/src` is under `project/src`**: 33 tests left `app/src` (the per-package `--list`
before, less the one after), and all 33 appear in `cargo test -p letur-project --
--list`, by the same module path. The seven that are new are the `MemFiles` cases the
scope names, plus `files::tests::a_path_is_spelled_once_and_never_leaves_the_root`.

Moved from `app/src/document.rs` to `project/src/document.rs` (29):
`a_construct_outside_the_dialect_names_itself_and_its_line`,
`a_document_and_its_bibliography_read_as_one_asset`,
`a_document_and_its_images_read_as_two_assets`,
`a_document_that_names_no_section_has_no_parts`,
`a_failed_fetch_is_refused_in_the_terminals_own_words`,
`a_fetched_url_is_supplied_under_its_own_name_and_read_from_nowhere`,
`a_file_in_the_pane_is_named_the_way_the_master_would_name_it`,
`a_main_that_will_not_read_says_so_in_the_terminals_own_words`,
`a_master_in_a_subdirectory_is_not_this_roots_master`,
`a_master_whose_sections_are_missing_still_names_them`, `a_mermaid_fence_compiles`,
`a_missing_bibliography_names_the_path_the_line_and_the_reason`,
`a_missing_image_names_the_path_the_line_and_the_reason`,
`a_missing_section_names_the_path_the_line_and_the_reason`,
`a_path_named_twice_is_read_once`,
`a_section_is_named_beside_the_master_that_reads_it`,
`a_single_file_document_keeps_its_anchors_and_its_bytes`,
`a_url_image_is_read_from_nowhere_and_refused_in_the_engine_s_words`,
`discovery_is_every_markdown_that_names_a_section`,
`every_file_a_master_names_is_read_once_across_both_passes`,
`only_the_headings_the_pane_holds_become_anchors`,
`the_anchors_are_the_headings_of_the_file_the_pane_is_holding`,
`the_asset_list_survives_a_failed_compile_but_not_a_failed_parse`,
`the_main_is_the_one_master_or_the_first_of_several`,
`the_panes_buffer_stands_in_for_the_file_it_is_holding`,
`the_panes_unsaved_bibliography_reaches_the_compile`,
`the_refused_url_is_read_off_the_error_and_the_urls_off_the_walk`,
`the_sections_a_master_names_are_its_parts_in_master_order`,
`the_showcase_compiles_to_the_bytes_the_engine_writes`.

Moved from `app/src/remote.rs` to `project/src/remote.rs` (4):
`a_claim_waits_and_begin_drops_what_is_no_longer_named`,
`a_compile_reads_the_allowed_sites_and_promotes_the_generation_it_read`,
`the_line_names_the_sites_not_allowed_first_and_says_nothing_while_waiting`,
`the_site_is_the_lowercased_host_or_the_url_itself`.

**Gate 2.** `cargo clippy -p letur-project -- -D clippy::disallowed_methods -D
clippy::disallowed_types` passes, and was seen to fail on a probe calling
`Path::is_file` before the probe was removed; `cargo build -p letur-project --target
wasm32-unknown-unknown` succeeds. **Gate 3.** `cargo build -p letur --features driven`,
then `bun app/driver/drive.mjs`: 6 clauses, 6 passed, WebKit 605.1.15 on the real
binary. `bun app/typecheck.mjs` exits 0. **Gate 4.** The four `const`s: none under
`app/src`, four under `project/src`. **`spec-lint`**: 0 errors, as before the phase;
the specs' diff is 33 `app/src/` → `project/src/` substitutions inside backticks and
nothing else, checked by pairing `git diff --word-diff=porcelain`'s lines.

**Where the build departed from the plan, each for a reason found while building:**

- **The loops still guard on the absolute path.** The plan made `edited` root-relative
  and would have compared that; two projects each holding a `README.md` would then
  pass a leftover loop's guard. `Session` joins it onto the root instead, which is the
  comparison the loops made before, and the `Gate` cases are unchanged.
- **The delete is a closure `Preview::trash` is handed**, not `Files::remove` alone:
  the desktop's suite replaces the move to the Trash per call, and `Session::trash`'s
  signature, `main.rs` and the three clauses that hand in a double stay as they were.
- **`Files::locate` and a `read` of a located path** replaced a purely root-relative
  read, so `render_with`, `read_sections_with` and `read_assets_with` keep their closure
  seam and the sentences keep the desktop's absolute path by construction.
- **`default_output` stays a function** (`project/src/document.rs`), which
  `Preview::export_path` asks — the specs cite it by name.
- **Two additions to the crate's surface**: the four sentences are `pub const`, for the
  desktop's tests to assert against; and `Preview::hold`, `#[doc(hidden)]`, moves the
  pane without loading, for the one `Gate` case that must move it without a compile.
- **`clippy.toml` also lists the `Path` methods that stat** (`is_file`, `exists`,
  `canonicalize` …): the same calls as `std::fs`'s, reached from a path.
- **`http` is the crate's third dependency** — `remote::host` parses a URL with it,
  where `app` reached it as `ureq::http`. It was already in `Cargo.lock`.

### Round 3 — Phase 1 only — 2026-09-25 — the scope reviewer, resumed — **READY (converged)**

Zero blocking. The round-2 blocker — citations in shipped specs — was verified resolved
against the file: the close-out states the path-only rewrite as an exception to §6.1's
"leave it", scoped to backticked `file:symbol` path halves in
`specs/desktop_app_spec.md` and `specs/file_panel_spec.md`, checked by `git diff
--word-diff`. The reviewer confirmed the rewrite can succeed because the moved type keeps
its name, `Preview`. One non-blocking finding, folded in: this spec's own §2 cites
`render_with` and `Preview` by `app/src` path, so the same rewrite applies here.

The correctness and exit-gate reviewers had already returned READY in round 2 and were
not re-run: nothing folded in after their verdicts touched a clause they had raised.

On convergence: `status: accepted`, Phase 1 `reviewed: 2026-09-25`. Phases 2–4 are
unreviewed; each needs its own round before it is planned.

### Round 2 — Phase 1 only — 2026-09-25 — the same three reviewers, resumed — **NOT READY (1 blocking)**

**Round 1 split Phase 1**, so this round judged the *new* Phase 1 — the extraction of
`letur-project` — with the web app as Phase 2. All round-1 blockers were verified
resolved in the file by the reviewer that raised them.

- **Correctness:** READY. Confirmed against `app/src/preview.rs` that `absorb`,
  `status`, `state`, `entries`, `exportable`, `edit` and `take` touch no watch type,
  clock or channel; only `claim` reaches the claims channel, which stays in `app`.
  Non-blocking, all folded in: gate 3 could pass on a stale binary (build step now named);
  `clippy.toml` cannot ban a module (list now enumerated); the typedef test's
  `include_str!` path only resolves from `app/src` (test stays in `app`); the
  `serde_json` lock wording.
- **Exit gate:** READY. Checked `cargo check --locked -p md2pdf-core --target
  wasm32-unknown-unknown` against the root lock (succeeded, 52 s), which answers gate
  2's feasibility. Confirmed `md2pdf-cli` and `md2pdf-core` versions move in lockstep
  (0.4.0→0.4.0, 0.1.3→0.1.3), which Phase 2's CLI pin relies on. Confirmed gate 4's grep
  matches four today. Non-blocking, folded in: `SystemTime::now` added to the clippy
  list; gate 1 records the moved test names, not only the count, so a dropped test
  cannot hide behind a new one.
- **Scope:** NOT READY. **[BLOCKING]** about fifty citations in two shipped specs name
  moving symbols by `app/src` path, and `spec-lint` makes a symbol absent from a file
  that still exists an error (`CIT_SYMBOL_ABSENT`); the close-out's "no unresolved
  citation" clause left the policy to a guess. **Resolved** by the stated path-only
  rewrite (round 3). Non-blocking, folded in: gate 3 overstated what `drive.mjs` covers
  (it covers the chrome, `open_document`, `set_appearance` and `fetch_images`, not save,
  switch or trash); the move list omitted sentence-bearing pieces its own constraint
  pulled in (`save_as`'s receipt, `Session`'s inline sentences, the rule halves of
  `confined`, `create_file` and `asset_bytes`); the constraint now excepts an I/O
  error's own `Display` tail.

### Round 1 — Phase 1 only — 2026-09-25 — a fresh panel of three (correctness, exit gate, scope) — **NOT READY (5 blocking, deduplicated)**

**Round 0**, answered by the author: yes — Phase 1 as drafted produced the observable
(Letur's window drawing the page beside the text, in a tab), and it was the right first
build, since the later phases only change where the files live. *The split below makes
the new Phase 1 an argued "no"; the question was asked of the phase as drafted, and is
not re-asked of the split.*

Blockers, and how each was resolved:

1. **The state machine was not extracted** (all three). §2 moved the pure helpers and
   the wire types, but every command answers through `app/src/preview.rs:Preview` and
   `Session` — `SWITCHING`, `TRASHING`, `SAVED`, `exportable`, the counters — so the web
   session would have been a second, unchecked implementation. **Resolved**: a `Files`
   trait and `MemFiles`, `Preview`'s pure core and `Session`'s pure decisions move to
   `letur-project`, with the duration passed in.
2. **`serde-wasm-bindgen` turns `None` into `undefined`** (correctness, exit gate). The
   window hides three bars on `=== null`, so every status would show them, and the
   typedef test compares names only. **Resolved**: JSON strings from `serde_json`; a
   Phase 2 gate clause checks the bars are hidden.
3. **The reference CLI was not pinned** (exit gate). The machine reviewed on carries
   `md2pdf` 0.1.3, which refuses task lists. **Resolved** in Phase 2's gate.
4. **The close-out missed three rules** (scope): `rules/desktop.md`,
   `desktop-panes.md` and `desktop-panel.md` cite moving symbols. **Resolved**, and
   "repoint" became "add", since most of `desktop-compile.md`'s subject stays in `app`.
5. **Phase 1 was two passes** (scope): a ~9,000-line desktop refactor and a new front
   end, with two blast radii in one gate. **Resolved** by splitting at the extraction;
   nothing was reviewed or shipped, so renumbering was legal. The phases are now four.

Also folded in: a full table for all nineteen commands and eight events (five commands
and four events had no stated answer), download tokens for save-as and export, the host
emitting `rendered` in place of the watch loop, the seed routed through `pending_open`
on the main thread with `CSS.escape`, the correction of "the window's own delete
confirmation stays" (the window confirms nothing, by design), OQ-6 resolved and OQ-7
added. **Nothing was rejected.**
